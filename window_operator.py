class WindowOperator():
    def __init__(self, window) -> None:
        self.window = window
    
    def window_visibility(window):
        if window.isHidden():
            window.show()
        else:
            window.hide()